This code trains a machine learning model to play cartpole using algorithms. 

Required dependencies:
torch 
gymnasium
numpy

Installation:
pip install torch gymnasium numpy
pip install gymnasium[classic-control]

How to run:
save code as .py
in terminal run: python [file name].py

To run without the render scroll to line 204 and change render=False
